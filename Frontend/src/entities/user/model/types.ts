/**
 * AegisQuiz — User Entity Types
 * =============================
 * FSD Layer: entities/user/model/types.ts
 */

export type {
  AppRoleType,
  UserProfile,
} from './schema';

export type {
  PermissionType,
} from '@/types/auth';
