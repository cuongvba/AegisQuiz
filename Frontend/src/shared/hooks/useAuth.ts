/**
 * AegisQuiz — useAuth Hook
 * ========================
 * Standard hook accessing AuthContext.
 * FSD Layer: shared/hooks/
 */

export { useAuthContext as useAuth } from '@/app/providers/AuthProvider';
export type { AuthContextValue } from '@/app/providers/AuthProvider';
